{
  "name": "scada-simulator-server",
  "version": "0.1.0",
  "private": true,
  "description": "Tag service, simulation engine, alarm engine and screen store for the WOIS SCADA training simulator",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "generate": "node ../tools/generate-tags.mjs && node ../tools/generate-screens.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
